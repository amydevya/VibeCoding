---
alwaysApply: true
---
# 任务管理工作流

## 配置区域

```yaml
project:
  name: "智能数据分析助理"
  team: "NL2SQL"  # Linear Team Key
  repo: "https://github.com/amydevya/VibeCoding.git"
```

---

## 工作流程

当用户使用日常口语描述任务意图时，按以下流程处理：

### 1. 意图识别

识别用户的操作意图：

| 用户说法 | 意图 | 动作 |
|---------|------|------|
| "开始做/开发/实现 XXX" | 开始任务 | 查找/创建 Issue → 更新状态为 In Progress → 推荐分支 |
| "完成/做完 XXX" | 完成任务 | 更新状态为 Done → 提醒创建 PR |
| "暂停/先不做 XXX" | 暂停任务 | 更新状态为 Todo/Backlog |
| "查看 XXX 的进度/状态" | 查询状态 | 列出相关 Issue 及当前状态 |
| "创建/新建任务 XXX" | 创建任务 | 在 Linear 创建新 Issue |
| "XXX 有问题/阻塞了" | 标记阻塞 | 更新 Issue 添加阻塞标签/评论 |
| "同步计划到 Linear" | 计划同步 | 解析 Markdown Phase 结构 → 创建父子 Issue |
| "同步 Todos 到 Linear" | Todos 同步 | 解析 frontmatter todos → 创建 Issue 结构 |

---

### 2. 开始任务流程

当用户说 "开始做 [功能名]" 时：

**Step 1: 查找或创建 Issue**

使用 `list_issues` 搜索相关任务：
```
tool: list_issues
arguments:
  query: "{功能关键词}"
  team: "{config.team}"
```

- 如果找到匹配 Issue → 使用该 Issue
- 如果未找到 → 使用 `create_issue` 创建新 Issue

**Step 2: 更新任务状态**

使用 `update_issue` 将状态设为 In Progress：
```
tool: update_issue
arguments:
  id: "{issue_id}"
  state: "In Progress"
  assignee: "me"
```

**Step 3: 推荐分支名称**

根据功能生成 Git 分支建议：

```
分支命名规范: {type}/{issue-id}-{short-desc}

示例：
- feat/ENG-123-login-feature
- fix/ENG-456-api-timeout
- refactor/ENG-789-database-layer
```

分支类型对照：
- `feat/` - 新功能
- `fix/` - Bug 修复
- `refactor/` - 重构
- `docs/` - 文档
- `test/` - 测试

**Step 4: 输出确认**

```
✅ 已更新任务状态

📋 Issue: [ENG-123] 登录功能开发
🔗 链接: {config.repo}/issues/123
📊 状态: Todo → In Progress

🌿 推荐分支:
   git checkout -b feat/ENG-123-login-feature

💡 下一步:
   1. 创建分支开始开发
   2. 完成后提交 PR
```

---

### 3. 完成任务流程

当用户说 "完成 [功能名]" 时：

**Step 1: 查找 Issue**
```
tool: list_issues
arguments:
  query: "{功能关键词}"
  team: "{config.team}"
  assignee: "me"
```

**Step 2: 更新状态**
```
tool: update_issue
arguments:
  id: "{issue_id}"
  state: "Done"
```

**Step 3: 输出确认**

```
✅ 任务已完成

📋 Issue: [ENG-123] 登录功能开发
📊 状态: In Progress → Done

🎉 提醒:
   - 确保代码已合并到主分支
   - 如有 PR，请确认已合并
```

---

### 4. 查询任务状态

当用户说 "查看 [功能] 的状态/进度" 时：

```
tool: list_issues
arguments:
  query: "{功能关键词}"
  team: "{config.team}"
```

输出格式：
```
📋 任务状态查询结果

1. [ENG-123] 登录功能开发
   状态: In Progress
   负责人: @me
   优先级: High
   更新: 2小时前

2. [ENG-124] 用户注册功能
   状态: Todo
   负责人: 未分配
   优先级: Normal
```

---

### 5. 创建新任务

当用户说 "创建/新建任务 [任务名]" 时：

```
tool: create_issue
arguments:
  title: "{任务标题}"
  team: "{config.team}"
  description: "{任务描述（如有）}"
  assignee: "me"
```

---

## 可用的 Linear 工具

| 工具 | 用途 |
|------|------|
| `list_issues` | 搜索/列出任务 |
| `get_issue` | 获取任务详情 |
| `create_issue` | 创建新任务 |
| `update_issue` | 更新任务（状态、负责人等） |
| `list_issue_statuses` | 获取可用状态列表 |
| `create_comment` | 添加任务评论 |

---

## 状态映射

| 语境 | Linear 状态 |
|------|-------------|
| 开始/进行中 | `In Progress` |
| 完成/做完 | `Done` |
| 暂停/待做 | `Todo` |
| 审核中 | `In Review` |
| 取消 | `Canceled` |

---

### 6. Markdown 计划同步到 Linear

当用户提供 Markdown 格式的开发计划时，自动解析并同步到 Linear。

#### 支持的格式

```markdown
## Phase 1: 基础框架搭建
- [ ] 创建后端项目结构
- [ ] 配置 FastAPI 应用
- [ ] 创建前端项目
- [ ] 健康检查测试

## Phase 2: 前端 UI 开发
- [ ] 左侧会话列表组件
- [ ] 中间聊天区域
- [ ] 右侧图表展示

## Phase 3: 后端接口开发
- [ ] LLM 接入模块
- [ ] 数据库管理
- [ ] 会话管理 API
```

#### 解析规则

| Markdown 元素 | Linear 对应 |
|--------------|-------------|
| `## Phase N: 标题` | 创建父 Issue（Phase 主任务） |
| `- [ ] 任务描述` | 创建子 Issue，关联到 Phase |
| `- [x] 已完成任务` | 状态设为 Done |

#### 同步流程

**Step 1: 解析 Markdown 结构**

识别所有 Phase 和对应的子任务列表。

**Step 2: 创建 Phase 父 Issue**

```
tool: create_issue
arguments:
  title: "Phase {N}: {Phase标题}"
  team: "{config.team}"
  project: "{config.project.name}"
  description: |
    ## 子任务
    {原始 Markdown 列表}
  labels: ["phase"]
```

保存返回的 Issue ID 作为 `parent_id`。

**Step 3: 创建子任务 Issue**

遍历每个子任务：
```
tool: create_issue
arguments:
  title: "{子任务标题}"
  team: "{config.team}"
  project: "{config.project.name}"
  parentId: "{phase_issue_id}"
  assignee: "me"
```

**Step 4: 输出同步结果**

```
✅ 计划已同步到 Linear

📦 Phase 1: 基础框架搭建
   └── [ENG-101] Phase 1: 基础框架搭建 (父任务)
       ├── [ENG-102] 创建后端项目结构
       ├── [ENG-103] 配置 FastAPI 应用
       ├── [ENG-104] 创建前端项目
       └── [ENG-105] 健康检查测试

📦 Phase 2: 前端 UI 开发
   └── [ENG-106] Phase 2: 前端 UI 开发 (父任务)
       ├── [ENG-107] 左侧会话列表组件
       ├── [ENG-108] 中间聊天区域
       └── [ENG-109] 右侧图表展示

📊 统计: 3 个 Phase, 11 个子任务

🔗 查看项目: {Linear 项目链接}
```

#### 触发关键词

| 用户说法 | 动作 |
|---------|------|
| "同步计划到 Linear" | 解析当前对话中的 Markdown 计划并同步 |
| "把这个计划同步到 Linear" | 同步指定的计划 |
| "帮我创建这些任务" | 创建 Issue 结构 |

---

### 7. Frontmatter Todos 同步到 Linear

当用户要求同步 Cursor Plans 文件中的 frontmatter todos 到 Linear 时使用此功能。

#### Frontmatter Todos 格式

```yaml
---
todos:
  - id: phase1-backend-init
    content: "Phase 1: 后端基础框架 - 创建项目结构、配置 FastAPI"
    status: pending
  - id: phase1-frontend-init
    content: "Phase 1: 前端基础框架 - 创建 React 项目、配置 Vite"
    status: pending
  - id: phase2-sidebar
    content: "Phase 2: 左侧边栏 UI - 会话列表组件"
    status: pending
---
```

#### 解析规则

| Frontmatter 字段 | 说明 |
|-----------------|------|
| `id` | 任务唯一标识，用于去重 |
| `content` | 任务标题，格式通常为 "Phase N: 任务描述" |
| `status` | `pending` → Todo, `in_progress` → In Progress, `completed` → Done |

#### 同步流程

**Step 1: 解析 Frontmatter Todos**

识别所有 todos 条目，按 Phase 分组：
- 提取 Phase 编号（从 content 中匹配 "Phase N"）
- 同一 Phase 的任务归为一组

**Step 2: 创建 Phase 父 Issue**

按 Phase 分组创建父 Issue：
```
tool: create_issue
arguments:
  title: "Phase {N}: {Phase主题}"
  team: "{config.team}"
  project: "{config.project.name}"
  labels: ["phase"]
  assignee: "me"
```

**Step 3: 创建子任务 Issue**

为每个 todo 创建子 Issue：
```
tool: create_issue
arguments:
  title: "{todo.content}"
  team: "{config.team}"
  project: "{config.project.name}"
  parentId: "{phase_issue_id}"
  assignee: "me"
```

**Step 4: 输出同步结果**

```
✅ Todos 已同步到 Linear

📦 Phase 1: 前后端基础框架搭建
   └── [NL2SQL-101] Phase 1: 前后端基础框架搭建 (父任务)
       ├── [NL2SQL-102] 后端基础框架 - 创建项目结构、配置 FastAPI
       ├── [NL2SQL-103] 前端基础框架 - 创建 React 项目、配置 Vite
       └── [NL2SQL-104] 健康测试 - 前后端启动验证

📦 Phase 2: 前端 UI 研发
   └── [NL2SQL-105] Phase 2: 前端 UI 研发 (父任务)
       ├── [NL2SQL-106] 左侧边栏 UI
       ├── [NL2SQL-107] 中间聊天 UI
       └── [NL2SQL-108] 右侧图表 UI

📊 统计: 4 个 Phase, 13 个子任务
```

#### 触发关键词

| 用户说法 | 动作 |
|---------|------|
| "同步 Todos 到 Linear" | 读取当前计划文件并同步 |
| "把 plan 里的 todos 同步到 Linear" | 同步指定计划文件的 todos |
| "帮我创建这些任务" | 创建 Issue 结构 |

---

## 使用示例

```
用户: 开始做用户登录功能
→ 搜索 Issue → 更新为 In Progress → 推荐: feat/ENG-123-user-login

用户: 登录功能做完了
→ 查找 Issue → 更新为 Done

用户: 查看数据可视化功能的进度
→ 列出相关 Issues 及状态

用户: 创建一个新任务：导出报表功能
→ 创建新 Issue，分配给自己

用户: 帮我把这个计划同步到 Linear
（用户提供 Markdown 格式的 Phase 计划）
→ 解析 Phase 结构 → 创建父 Issue → 创建子 Issue → 建立关联

用户: 把 plan 文件中的 Todos 同步到 Linear
→ 读取 .cursor/plans/*.plan.md → 解析 frontmatter todos → 按 Phase 分组 → 创建父子 Issue
```
